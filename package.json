{
  "name": "e2e-tester",
  "version": "1.0.0",
  "description": "E2E testing application with React frontend, Express backend, and Playwright testing",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "npm run verify-setup && playwright test",
    "test:headed": "npm run verify-setup && playwright test --headed",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "verify-setup": "./scripts/health-check.sh",
    "docker:start": "docker-compose up -d backend && sleep 5 && docker-compose up -d frontend",
    "docker:test": "npm run verify-setup && docker-compose --profile test up"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0",
    "concurrently": "^8.2.2"
  }
}
